# Install cask
brew update
brew tap homebrew/cask

# Cask syntax was changed to --cask flag

brew tap homebrew/cask-versions

# java 11 (for m1 and intel macs)
# brew install --cask zulu11

# java 17 (most repos are now requiring java17)
# brew install --cask zulu17
